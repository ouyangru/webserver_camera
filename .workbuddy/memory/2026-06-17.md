# 2026-06-17

- 将硬编码绝对路径 `/usr/share/webserver_camera/resources` 改为运行时自动推导的相对路径：http_conn 使用 `set_doc_root()`，main 通过 `/proc/self/exe` 推导路径，rtsp_server 使用 `set_hls_resources_dir()`，hls_transcode.sh 使用 `./resources/hls`
- 重写 `resources/index.html` 为完整视频监控仪表盘，支持 MJPEG/HTTP-FLV/HLS 三路流切换、实时状态面板、FLV 源 API 控制、事件日志、快捷链接、全屏播放等功能
